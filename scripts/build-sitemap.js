const fs=require('fs'),path=require('path'),c=require('cheerio');function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>['node_modules','tmp','.git'].includes(e.name)?[]:e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]);}
const urls=[];for(const f of walk('.').filter(f=>f.endsWith('.html'))){const $=c.load(fs.readFileSync(f,'utf8'));if(($('meta[name="robots"]').attr('content')||'').includes('noindex'))continue;const u=$('link[rel="canonical"]').attr('href');if(u)urls.push(u);}
fs.writeFileSync('sitemap.xml','<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n'+[...new Set(urls)].sort().map(u=>`  <url><loc>${u}</loc><lastmod>2026-09-11</lastmod></url>`).join('\n')+'\n</urlset>\n');console.log(urls.length+' sitemap URLs');
